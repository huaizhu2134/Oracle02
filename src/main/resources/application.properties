# Application Configuration
spring.application.name=playmate-admin

# Server Configuration
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/dfhz001?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=qq2134769644
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Druid Connection Pool Configuration
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=org.example.oracle01.model
mybatis.configuration.map-underscore-to-camel-case=true

# Disable Spring Session JDBC auto-configuration
spring.session.store-type=none

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.example.oracle01.mapper=DEBUG
logging.level.com.alibaba.druid=DEBUG
logging.level.root=INFO
